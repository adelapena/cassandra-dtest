#
# Sleep 5s during index update
#
RULE sleep 5s on index update
CLASS org.apache.cassandra.index.SecondaryIndexManager$WriteTimeTransaction
METHOD onUpdated
AT ENTRY
# set flag to only run this rule once.
IF NOT flagged("done")
DO
   flag("done");
   Thread.sleep(5000)
ENDRULE
